# Platform Components

Shared room/lobby/device-mode components used by every game (per the
`GameModule` contract, ADR-0002). Individual games consume these; they do not
reimplement room or device-mode UI themselves.
